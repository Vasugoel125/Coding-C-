#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	int remainder;
	string result ="";
	while(n!=0)
	{
		remainder = n%2;
		result += to_string(remainder);
		n = n/2;
	}
	reverse(result.begin(),result.end());
	cout<<result;
	return 0;
}
