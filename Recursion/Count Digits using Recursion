#include<bits/stdc++.h>
using namespace std;
int sum(int n)
{
    if(n/10==0)
    {
        return 0;
    }
    return sum(n/10) + 1;
}
int main()
{
    int n;
    cin>>n;
    cout<<sum(n);
    return 0;
}
